require 'pp'
require_relative "../utils/maybe"

describe "maybe" do
  it "should select an item"

  it "should select highest probability item more often"
    # Only way I think I can test this is to have a
    # probability of 0.

  it "should handle nested maybes"
end
